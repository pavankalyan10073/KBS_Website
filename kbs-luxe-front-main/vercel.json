{
  "build": {
    "cwd": "kbs-luxe-front-main",
    "outputDirectory": ".output",
    "installCommand": "bun install --frozen-lockfile",
    "devCommand": "bun run dev",
    "buildCommand": "bun run build"
  }
}